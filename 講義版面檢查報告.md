# 講義版面檢查與修正報告

檢查日期：2026-07-20　　範圍：ch1–ch26 中英文各一份，共 **52 份 revealjs 講義、1305 頁**

---

## 一、結論

| 項目 | 修正前 | 修正後 |
|---|---|---|
| YAML 設定一致性 | ✅ 52 份完全一致 | ✅ 維持一致 |
| 缺圖／斷掉的圖片路徑 | ✅ 0 筆 | ✅ 0 筆 |
| R 執行錯誤殘留 | ✅ 0 筆（見第四節） | ✅ 0 筆 |
| 引用未解析 | ✅ 0 筆 | ✅ 0 筆 |
| 空白頁 | ✅ 0 筆（見第四節） | ✅ 0 筆 |
| **內容溢出下緣** | ⚠️ **146 頁（11.2%）** | ✅ **0 頁** |
| **內容溢出右緣** | ⚠️ **10 頁** | ✅ **0 頁** |
| 圖片變形 | — | ✅ 0 頁（全部維持原長寬比） |

「跑掉」的成因不是格式亂跳，而是**單頁塞太多東西**。投影片固定 1050×700，超出的部分放映時直接看不到，或壓在頁尾字樣上。修正後 1305 頁全部收進邊界，已逐頁在無頭瀏覽器實測確認。

**⚠️ 需要重新 render 才會生效**：修改的是 `.qmd` 與 `custom.scss`，現有的 `.html` 尚未更新。

---

## 二、做了哪些修改

### 1. 新增 `custom.scss`（26 個章節資料夾各一份）

集中處理全域性的版面問題，內容都經過實測挑選——**只保留確認能降低內容高度的規則**，過程中發現兩個反效果的寫法並已避開（見第五節）。主要規則有四類：R 繪圖等比例限高（單圖 330px、多圖 210px）、表格依列數自動縮字、文字垂直間距收緊、程式碼行高微收。

### 2. 52 份 `.qmd` 的 YAML

```yaml
theme: serif                      →    theme: [serif, custom.scss]
```

### 3. 94 處投影片標題加上版面類別

全域規則處理不掉的頁面，逐頁指定。**只在標題後加一個標記，完全沒有更動任何教學內容、程式碼或文字**：

```markdown
## 運算順序 {.tight}
## qqmath on Prices: Finding the Log-Normal {.tight .fit-e}
```

| 類別 | 作用 | 用量 |
|---|---|---|
| `.tight` | 內文字級 28px → 24px（標題不變） | 58 頁 |
| `.tight .fit-a` | 加上繪圖限高 200px | 10 頁 |
| `.tight .fit-b` | 繪圖限高 160px | 9 頁 |
| `.tight .wrap` | 長程式碼行改為換行 | 6 頁 |
| `.tight .fit-d` | 繪圖限高 100px | 5 頁 |
| `.tight .fit-e` | 繪圖限高 78px | 3 頁 |
| `.wrap` | 長程式碼行改為換行 | 2 頁 |
| `.tight .fit-c` | 繪圖限高 125px | 1 頁 |

類別選擇器在 `custom.scss` 中刻意重複兩次（`.fit-a.fit-a`）以提高特異度，確保勝過全域規則。若日後想微調某頁，直接改標題上的類別即可，不必動 CSS。

---

## 三、修正效果與仍待斟酌之處

修正前最嚴重的是 ch14 p18，內容比投影片高出 1290px——放映時只看得到標題和第一張圖，另外兩張圖與兩段解說完全消失。修正後三張圖、三段程式碼、兩段說明全部進得來。ch6 p14 的 20 列運算子優先序表原本只看得到 11 列，現在完整呈現，連結尾那句 `help(syntax)` 也回來了。

**但有 8 頁的圖被壓得偏小**，雖然收進邊界了，座標軸標籤在投影時可能不易辨識。這些頁面本質上是「三張圖＋三段程式碼」擠一頁，CSS 只能縮小、無法真正解決。**建議日後有空時拆成兩頁**：

| 講義 | 頁 | 標題 | 目前類別 |
|---|---|---|---|
| `ch14` / `ch14-zh` | 18 | qqmath on Prices: Finding the Log-Normal | `.fit-e`（圖高 78px） |
| `ch16` | 12 | loadings, Scree Plot, Biplot | `.fit-e` |
| `ch14` / `ch14-zh` | 4 | A Simple Example: Formula, Conditioning | `.fit-d`（圖高 100px） |
| `ch15` / `ch15-zh` | 4 | Facets and Colors | `.fit-d` |
| `ch16-zh` | 12 | loadings、碎石圖、雙標圖 | `.fit-d` |

另一個等效做法是在這幾頁的 chunk 加 `fig.show='hold', out.width='50%'` 讓兩張圖並排，比上下堆疊更適合對照比較——但這牽涉 chunk 行為改變，這次沒有動。

原本 146 頁溢出中，各章分布以 ch14（23 頁）、ch18（15 頁）、ch6/ch7（各 12 頁）、ch13（10 頁）最多；ch1–ch3、ch20、ch21、ch24–ch26 原本就完全乾淨，修正後仍乾淨且版面更寬裕。英文版比中文版容易溢出（82 頁 vs 64 頁），因為相同語意下英文展開較長。

---

## 四、確認為「非問題」的項目

初步掃描曾標記兩類疑似錯誤，逐一核對後**確認都不是問題**，記錄於此以免日後重複檢查。

**R 錯誤訊息（34 頁）** — 全是刻意的教學示範。ch4–ch10 共有 32 個 chunk 使用舊式語法 `{r,error=TRUE}`，示範「套件沒載入會怎樣」「大括號不建立環境」「stop/warning/try 如何運作」等。例如 ch4 p9 先示範 `rpart()` 找不到函式，接著才 `library(rpart)`，是有意的鋪陳。（初次 grep 找 `error: true` 得到 0 筆，是因為這些用的是舊式行內語法。）

**空白頁（14 頁）** — 是 level-1 章節分隔頁（只含一個 `<h1>`），像「向量」「符號」「轉換」，屬 Quarto revealjs 正常的章節過場。只有中文版被標記是因為偵測用了字元長度門檻，中文標題兩三個字就低於門檻，屬誤判。

---

## 五、附錄：方法與過程中的兩個陷阱

沒有採用估算，而是**實際渲染量測**：在無頭 Chromium 中載入 52 份 HTML，透過 Reveal API 逐頁切換，等版面穩定後讀取該頁所有元素的實際 bounding box，換算回投影片邏輯座標（1050×700、`center: false` 內容靠上），計算內容底緣與右緣超出邊界的像素數，並比對圖片顯示尺寸與原始長寬比以偵測變形。修正後以同樣方式重測全部 1305 頁。

版面參數取自主題 CSS 實測值：內文 28px（`--r-main-font-size: 40px` × `smaller` 的 0.7）、行高 1.3、區塊間距 12px、程式碼 15.4px／行高 20px。

過程中有兩個反直覺的發現，寫在這裡供日後參考：

**其一，替 `pre` 或 `div.sourceCode` 加 margin 會讓頁面高度暴增**（實測單頁 +115px），與直覺相反。因此 `custom.scss` 只調行高，完全不碰這兩者的 margin。

**其二，`:has()` 不能巢狀。** 原本寫 `section:has(.cell:has(img) ~ .cell:has(img))` 想涵蓋跨 chunk 的多圖情形，結果整條規則（含逗號前另一個合法的選擇器）被瀏覽器判定無效而整組丟棄，導致多圖限高一直沒生效。改成兩條獨立選擇器後才正常。

判定結果另以螢幕截圖交叉驗證，涵蓋各類成因與正常對照組，修正前後皆確認無誤。
