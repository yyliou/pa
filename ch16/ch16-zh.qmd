---
title: "程式設計與應用"
subtitle: "第十六章：分析資料"
author: "劉昱佑"
institute: "實踐大學"
date: "`r Sys.Date()`"
format: revealjs
smaller: true
theme: serif
footer: "R in a Nutshell: A Desktop Quick Reference"
logo: "logo.svg"
editor: source
bibliography: bib.bib
suppress-bibliography: true
slide-number: true
scrollable: false
transition: "none"
menu:
  useTextContentForMissingTitles: false
  hideMissingTitles: true
  side: 'left'
  titleSelector: 'h1'
title-slide-attributes:
  data-notes: ""
include-after-body:
  text: |
    <script>
    Reveal.on('ready', function() {
      var title = Reveal.getSlides()[0];
      if (title) {
        title.insertAdjacentHTML('beforeend',
          '<p style="font-size:0.5em; margin-top:1.5em;">' +
          '<a href="ch16.html"  style="color:#555;">[English version]</a>' +
          '</p>');
      }
    });
    </script>
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(fig.align = "center", fig.height = 4.2)
```

# **第一輪分析**

## 摘要統計量：mean、min、max

之後一切的積木。用第十二章的 `dow30` 報價資料（收錄於 `nutshell`）：

```{r,echo=TRUE}
temp <- tempfile(fileext = ".rda")
download.file("https://raw.githubusercontent.com/cran/nutshell/master/data/dow30.rda",
              temp, mode = "wb")
load(temp)
mean(dow30$Open); min(dow30$Open); max(dow30$Open)
```

兩個值得背下來的引數：

```{r,echo=TRUE}
mean(c(1, 2, 3, 4, 5, NA))                  # 一個 NA 就毒死整個結果……
mean(c(1, 2, 3, 4, 5, NA), na.rm=TRUE)      # ……除非把它剔除
mean(c(-1, 0:100, 2000))                    # 離群值拖著平均跑……
mean(c(-1, 0:100, 2000), trim=0.1)          # ……trim 過濾掉這個比例
```

## range、quantile、fivenum、IQR

```{r,echo=TRUE}
range(dow30$Open)
quantile(dow30$Open, probs=c(0, 0.25, 0.5, 0.75, 1.0))
fivenum(dow30$Open)
IQR(dow30$Open)
```

這些函式都能與 `apply`／`tapply` 自然搭配，逐組計算統計量（第十二章）。

## summary 與 str

最方便的總覽是泛型的 `summary`——數值給四分位數＋平均、因子給高頻計數（純字元向量則沒有有意義的資訊）：

```{r,echo=TRUE}
summary(dow30[, c("Open", "Volume")])
```

熱門替代品 `str` 顯示物件的*結構*：

```{r,echo=TRUE}
str(dow30)
```

## stem：文字版直方圖

`stem(x, scale=1, width=80, atom=1e-08)` 直接在主控台畫莖葉圖。2005 年 NFL *失手*射門的距離：

```{r,echo=TRUE}
temp <- tempfile(fileext = ".rda")
download.file("https://raw.githubusercontent.com/cran/nutshell/master/data/field.goals.rda",
              temp, mode = "wb")
load(temp)
stem(subset(field.goals, play.type=="FG no")$yards)
```

# 相關與共變異

## 三種相關係數

相關回答「x 上升時 y 是否跟著上升、咬得多緊？」，以 −1…1 度量**線性相依**。三種統計量：

- **Pearson**——Excel 的 `CORREL`；植根於常態理論，最適合近似常態的資料；
- **Spearman**——無母數、不做分布假設（對*等級*做相關）；
- **Kendall 的 tau**——比較*成對排序*的一致與不一致對數。

```{r,eval=FALSE,echo=TRUE}
cor(x, y = NULL, use = "everything",
    method = c("pearson", "kendall", "spearman"))
```

`x`、`y` 可以是兩個向量，或一個資料框／矩陣（→ 相關*矩陣*）。`use` 處理 `NA`：`"all.obs"`（報錯）、`"everything"`（結果為 NA）、`"complete.obs"`／`"na.or.complete"`（丟不完整列）、`"pairwise.complete.obs"`（逐對丟）。

## 範例：孕期增重與出生體重

母親增重與寶寶體重相關嗎？先清理（有效值、單胞胎、妊娠 > 35 週），再看圖——十萬多點得用 `smoothScatter`、不能用 `plot`：

```{r,echo=TRUE}
temp <- tempfile(fileext = ".rda")
download.file("https://raw.githubusercontent.com/cran/nutshell/master/data/births2006.smpl.rda",
              temp, mode = "wb")
load(temp)
births2006.cln <- births2006.smpl[
  !is.na(births2006.smpl$WTGAIN) & !is.na(births2006.smpl$DBWT) &
  births2006.smpl$DPLURAL == "1 Single" & births2006.smpl$ESTGEST>35,]
smoothScatter(births2006.cln$WTGAIN, births2006.cln$DBWT)
cor(births2006.cln$WTGAIN, births2006.cln$DBWT)
cor(births2006.cln$WTGAIN, births2006.cln$DBWT, method="spearman")
```

兩種度量都顯示中等偏弱的相關——那團雲確實有點傾角，但只是一點。（書中用的是 320 萬筆的完整檔；我們的 10% 樣本給出幾乎相同的值。）

## 共變異

**共變異（covariance）**就是 Pearson 公式的分子——引數與 `cor` 相同：

```{r,echo=TRUE}
cov(births2006.cln$WTGAIN, births2006.cln$DBWT)
```

親戚：`cov2cor` 把共變異矩陣轉成相關矩陣；`cov.wt(x, wt=, cor=, center=, method=c("unbiased","ML"))` 計算*加權*共變異。

# 主成分分析

## prcomp 與 princomp

**PCA** 把（可能相關的）變數改寫成互不相關的成分。兩個實作：`prcomp`（建議使用；公式法 `prcomp(~..., data=, subset=, na.action=)`，預設法有 `retx`、`center=TRUE`、`scale.`、`tol`）與 S-PLUS 相容的 `princomp`（對相關／共變異矩陣做特徵分解）——書中示範的是後者。

資料：2000–2008 各隊打擊統計，從 Baseball Databank 以 SQL 取出（縮寫改成易讀的名稱），打包為 `team.batting.00to08`：

```{r,echo=TRUE}
temp <- tempfile(fileext = ".rda")
download.file("https://raw.githubusercontent.com/cran/nutshell/master/data/team.batting.00to08.rda",
              temp, mode = "wb")
load(temp)
batting.pca <- princomp(~singles+doubles+triples+homeruns+
                          walks+hitbypitch+sacrificeflies+
                          stolenbases+caughtstealing,
                        data=team.batting.00to08)
summary(batting.pca)
```

兩個成分已扛起約 78% 的變異；四個約 92%。

## loadings、碎石圖、雙標圖

`loadings` 顯示各變數對各成分的貢獻：

```{r,echo=TRUE}
loadings(batting.pca)
```

```{r,echo=TRUE}
plot(batting.pca)                                      # 「碎石」圖
biplot(batting.pca, cex=0.5, col=c("gray50", "black")) # 變數＋觀測
```

雙標圖把變數對成分 1–2 的貢獻*與*觀測畫在同一尺度上：一壘安打與保送主宰前兩個成分。（這份資料在第二十章的線性模型再登場。）

# 因素分析

## factanal

有些量觀測得到（測驗分數）、有些不行（智力）。**因素分析**從觀測變數推測隱藏因素——`stats` 的 `factanal`：

```{r,eval=FALSE,echo=TRUE}
factanal(x, factors, data = NULL, covmat = NULL, n.obs = NA,
         subset, na.action, start = NULL,
         scores = c("none", "regression", "Bartlett"),
         rotation = "varimax", control = NULL, ...)
```

| 引數 | 說明 |
|---|---|
| `x`／`data` | 公式或數值矩陣（公式時加資料框） |
| `factors` | 要配適幾個因素 |
| `covmat`／`n.obs` | 也可給共變異矩陣（或 `cov.wt` 輸出）＋觀測數 |
| `subset`／`na.action`／`start` | 老面孔 |
| `scores` | `"none"`、`"regression"`（Thompson）、`"Bartlett"`（加權最小平方） |
| `rotation` | 因素旋轉函式，預設 `"varimax"` |

# 拔靴重抽樣

## 想法，以及 boot

你的統計量對少數離群值敏感嗎？它的抽樣範圍多大？**拔靴法（bootstrap）**對*任意*統計量都能回答：反覆**放回**抽樣、重算統計量、觀察其散布——正式地說，是估計一個估計量的**偏誤**。

```{r,eval=FALSE,echo=TRUE}
library(boot)
boot(data, statistic, R, sim="ordinary", stype="i",
     strata=rep(1,n), L=NULL, m=0, weights=NULL,
     ran.gen=function(d, p) d, mle=NULL, simple=FALSE, ...)
```

重點引數：`data`；`statistic`——一個*（資料, 選擇）的函式*，第二個引數的意義由 `stype` 決定（`"i"` 索引、`"f"` 次數、`"w"` 權重）；`R`——重抽次數；`sim`——`"ordinary"`、`"parametric"`、`"balanced"`、`"permutation"`、`"antithetic"`；多樣本問題用 `strata`；參數化模擬用 `ran.gen`／`mle`；`simple=TRUE` 以時間換記憶體。

## 範例：中位數有多偏？

媒體報導房價*中位數*。對 2008 年 6 月舊金山成交價拔靴：

```{r,echo=TRUE}
temp <- tempfile(fileext = ".rda")
download.file("https://raw.githubusercontent.com/cran/nutshell/master/data/sanfrancisco.home.sales.rda",
              temp, mode = "wb")
load(temp)
home.sale.prices.june2008 <- subset(sanfrancisco.home.sales,
  format(date, "%Y-%m")=="2008-06")$price
library(boot)
b <- boot(data=home.sale.prices.june2008,
          statistic=function(d, i) { median(d[i]) },
          R=1000)
b
```

結論：在這裡中位數是非常輕微有偏的估計量——而且我們順便拿到了它的標準誤，完全不靠常態假設。

## 版權聲明

- **版權。** 本講義改編自 Joseph Adler 著、O'Reilly Media 出版之 *R in a Nutshell: A Desktop Quick Reference*（第二版）。原著作者與出版社保留一切權利。

- **僅限非商業用途。** 本教材僅供教學使用，不得用於商業營利。

- **標示出處。** 任何重製、散布或使用本教材，皆須妥善標註原始出處。
